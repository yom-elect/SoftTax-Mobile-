const INITIAL_STATE = {
    isSignedIn: false,
    token: null,
    
}



const loginReducer = (state= INITIAL_STATE, action)=>{
    switch (action.type) {
        default:
            return state
    }
}

export default loginReducer;